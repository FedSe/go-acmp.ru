package main
import . "fmt"
func main() {
	var (
		n, i, c int
		a [1000]int
	)

	Scan(&n)

	for i < n {
		Scan(&a[i])
	i++
	}

	Scan(&n)

	for 0 < n {
		Scan(&i, &c)
		for
		i--
		i < c
		{
			Println(a[i])
		i++
		}
	n--
	}
}