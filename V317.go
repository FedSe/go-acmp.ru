package main
import . "fmt"
func main() {
	var x, y, z, w, c, i int
	Scan(&x, &y, &z, &w)

	for i*x <= w {
		for
		j := 0
		j*y+i*x <= w
		{
			if ((w - i*x - j*y) / z)*z+j*y+i*x == w {
				c++
			}
		j++
		}
	i++
	}

	Print(c)
}